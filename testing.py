

#pytesseract.pytesseract.tesseract_cmd = "C:\\Program Files\\Tesseract-OCR\\tesseract.exe"
#img = cv2.imread(fname)
#gray_img = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)
#threshold_img = cv2.threshold(gray_img, 0, 255, cv2.THRESH_BINARY | cv2.THRESH_OTSU)[1]
#custom_config = r'-l hrv --psm 6'
#rez = pytesseract.image_to_string(threshold_img, lang="hrv", config=custom_config)
#self.T.insert(0.0, rez)

